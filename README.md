# App Preview Craft
A Python project for importing media from an Xcode project folder.

## Usage
1. Clone the repository.
2. Run the project using `python -m app_preview_craft --folder /path/to/xcode/project`.
3. The project will import the media and print the results in JSON format.
